name: my_reading_app
description: Manga Studio V23 - Fix HFS UI Scan.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  photo_view: ^0.15.0 
  google_fonts: ^6.1.0
  http: ^1.1.0 
  html: ^0.15.4
  archive: ^3.3.7

flutter:
  uses-material-design: true